[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"
  edge_functions = "netlify/edge-functions"

[build.environment]
  NODE_VERSION = "20"

# share-card uses satori (which loads its Yoga layout engine from a bundled
# yoga.wasm) and @resvg/resvg-wasm. esbuild can't inline .wasm assets, and
# bundling satori leaves yoga.wasm behind — the function then throws at load
# time in prod (a 502). Marking these external ships each package whole (wasm
# assets included) into the function bundle instead of inlining it.
[functions]
  node_bundler = "esbuild"
  external_node_modules = ["satori", "@resvg/resvg-wasm"]

[dev]
  command = "npm run dev"
  port = 8888
  targetPort = 5173
  framework = "#custom"
  envFile = ".env.local"

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://assets.lemonsqueezy.com https://app.lemonsqueezy.com; style-src 'self' 'unsafe-inline' https://assets.lemonsqueezy.com https://fonts.googleapis.com; img-src 'self' data: blob: https: http:; connect-src 'self' https://*.supabase.co wss://*.supabase.co https://api.anthropic.com https://*.lemonsqueezy.com https://thebooksoracle.lemonsqueezy.com https://api.lemonsqueezy.com https://openlibrary.org https://covers.openlibrary.org https://www.googleapis.com; frame-src https://*.lemonsqueezy.com https://thebooksoracle.lemonsqueezy.com; font-src 'self' https://fonts.gstatic.com data:; worker-src 'self' blob:;"

# v0.39: dynamic sitemap — must come before the SPA catch-all below so
# /sitemap.xml resolves to the function's XML instead of index.html.
[[redirects]]
  from = "/sitemap.xml"
  to = "/.netlify/functions/sitemap"
  status = 200

[[edge_functions]]
  path = "/*" # The URL path you want to hit
  function = "og-prerender"

# SPA fallback: any unknown route returns index.html so client-side
# routing (and the OAuth redirect after Google sign-in) works.
# Note: /.netlify/functions/* is automatically excluded.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200